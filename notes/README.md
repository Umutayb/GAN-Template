To get started:

Create `venv`
```shell
python -m venv venv
```
Activate venv
```shell
source venv/bin/activate
```
Install requirements
```shell
pip install -r requirements.txt 
```
Start training
```shell
python main.py
```
